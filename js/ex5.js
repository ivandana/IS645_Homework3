var number = parseInt(prompt("Enter a number:"));
var count = parseInt(prompt("Enter number"));

for (var i = 0; i <= count; i++) {

    const result = i * number;

    console.log(`${number} * ${i} = ${result}`);
    
}